name: build and deploy code

on: [push, pull_request]

jobs:
  job1:
    runs-on: ubuntu:latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4
      - name: say hello to razvan
        run: echo "hello razvan"